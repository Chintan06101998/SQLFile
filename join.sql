-- show databases;
-- Create DATABASE newJoinDatabase;
-- USE newJoinDatabase; 

-- CREATE TABLE users (
--     id INT AUTO_INCREMENT PRIMARY KEY,
--     name VARCHAR(50),
--     email VARCHAR(60)
-- );

-- CREATE TABLE purchases(
--     id INT AUTO_INCREMENT PRIMARY KEY,
--     order_date DATE,
--     amount INT,
--     user_id INT,
--     FOREIGN KEY(user_id) REFERENCES users(id)
-- );

-- DESC users;
-- DESC purchases;

-- Insert into users(name,email)
-- values('thor', 'thor@gmail.com'),
-- ('Peter', 'Peter@gmail.com'),
-- ('Tony', 'Tony@gmail.com'),
-- ('Natash', 'Natash@gmail.com');

-- Insert into purchases(order_date, amount, user_id)
-- values('2022-04-22',199,2);

